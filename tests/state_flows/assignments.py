def assignments(external_var):
    # Basic assignments
    a = 10
    
    # Multiple assignment and tuple unpacking
    x, y, z = 1, "test_string", True
    
    # Augmented assignments
    a += 5
    y *= 2
    
    b = a
    
    return b, x, y
